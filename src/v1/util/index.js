const sendRegistrationConfirmationEmail = require('./send-registration-confirmation-email')
const sendPasswordResetEmail = require('./send-password-reset-email')

module.exports = {
  sendRegistrationConfirmationEmail,
  sendPasswordResetEmail
}
