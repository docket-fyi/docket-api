// const events = require('./events')
const users = require('./users')
const me = require('./me')
const auth = require('./auth')
const docs = require('./docs')

module.exports = {
  // events,
  users,
  me,
  auth,
  docs
}
