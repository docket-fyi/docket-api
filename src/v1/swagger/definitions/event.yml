definitions:
  Event:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
      slug:
        type: string
      date:
        type: string
      userId:
        type: string
      createdAt:
        type: string
      updatedAt:
        type: string
      deletedAt:
        type: string
