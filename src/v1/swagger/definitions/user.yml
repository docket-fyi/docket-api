definitions:
  User:
    type: object
    properties:
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      preferredMeasurementUnit:
        type: string
      isPremium:
        type: string
      confirmedAt:
        type: string
      updatedAt:
        type: string
      deletedAt:
        type: string
