components:
  parameters:
    UserConfirmRegistrationCodeParameter:
      name: code
      in: query
      description: Confirm registration code parameter
      required: true
      schema:
        type: string
